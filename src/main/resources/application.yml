server:
  port: 8080

#postgres connection
spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/godis
    username: caolan
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop #destroy schema at end of session
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet
